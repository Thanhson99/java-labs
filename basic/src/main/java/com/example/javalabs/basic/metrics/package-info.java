/**
 * Fixed-memory metric windows and adaptive policies.
 *
 * <p>This package contains latency, throughput, error-rate, and timeout examples that show how to
 * keep operational metrics cheap enough for hot paths while still exposing useful decisions.</p>
 */
package com.example.javalabs.basic.metrics;

/**
 * Code-first Java practice examples used by the learning site.
 *
 * <p>The package starts with beginner-friendly syntax, records, collections, and object-oriented
 * examples, then grows into backend patterns such as caching, batching, retry, circuit breaker,
 * rate limiting, outbox processing, feature flags, observability, and operational alerting.</p>
 *
 * <p>Most classes are intentionally small and focused. Public APIs include JavaDoc so learners can
 * read the contract first, inspect the implementation second, and then confirm the behavior in the
 * matching unit tests.</p>
 */
package com.example.javalabs.basic;

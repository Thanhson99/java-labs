/**
 * Feature flag practice examples, from deterministic rollout to safe reload workflows.
 *
 * <p>This package keeps the feature flag code together so learners can follow one production
 * concern from basic percentage rollout through validation, audit, rollback, metrics, alerts,
 * dead-letter replay, and incident triage.</p>
 */
package com.example.javalabs.basic.featureflag;
